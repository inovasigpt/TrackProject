{
  "name": "pmo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "dev:web": "pnpm --filter @pmo/web dev",
    "dev:api": "pnpm --filter @pmo/api dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "hono": "^4.11.7"
  }
}
